<header class="navbar navbar-fixed-top navbar-inverse"id="navcolor">
  <div class="container">
    <%= link_to "PAU HANA CHI", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Home",   root_path %></li>
        <% if logged_in? && current_user.admin? %>
          <li><%= link_to "Specials", bars_path %></li>
          <li><%= link_to "Add Bar Special", bars_new_path %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"> Admin Controls <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Edit Users", users_path %></li>
              <li><%= link_to "Settings", edit_user_path(current_user) %></li>
              <li><%= link_to "Log out", logout_path, method: "delete" %></li>
        <% end %>
        <!-- begin Navbar for logged in user thats not admin-->
        <% if logged_in? && !current_user.admin? %>
          <li><%= link_to "Add Bar Special", bars_new_path %></li>
          <li><%= link_to "Specials", bars_path %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"> Account <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", current_user %></li>
              <li><%= link_to "Settings", edit_user_path(current_user) %></li>
              <li class="divider"></li>
              <li> <%= link_to "Log out", logout_path, method: "delete" %></li>
            </ul></li>
          <% end %>
          <% if !logged_in? %>
              <li><%= link_to "Log in", login_path %></li>
            <% end %>
          </ul>
      </nav>
  </div>
</header>
